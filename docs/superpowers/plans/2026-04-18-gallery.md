# Photo Gallery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a public `/galeria` route where guests can view and download all photos and videos uploaded to the R2 bucket.

**Architecture:** The existing Cloudflare Worker gets a new `GET /list` endpoint that calls `env.BODA_BUCKET.list()` and returns file metadata sorted newest-first. The Angular `GalleryComponent` fetches that list and renders a responsive CSS grid with hover overlays and direct R2 download links. R2 bucket public access must be enabled manually so downloads bypass the Worker.

**Tech Stack:** Angular 17 (standalone components), Cloudflare Worker (vanilla JS), Cloudflare R2

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `cloudflare-worker/index.js` | Modify | Add `GET /list` endpoint alongside existing `POST /` upload |
| `src/app/services/gallery.service.ts` | Create | HTTP call to Worker `/list`, returns typed file array |
| `src/app/components/gallery/gallery.component.ts` | Create | Fetch files, manage loading/error/empty state |
| `src/app/components/gallery/gallery.component.html` | Create | Grid template with hover overlays, download links |
| `src/app/components/gallery/gallery.component.css` | Create | Grid styles, hover transitions, responsive breakpoints |
| `src/app/app.routes.ts` | Modify | Add `/galeria` route |

---

## Task 1: Cloudflare Infrastructure — Enable R2 Public Access

> Manual task. No code. Must be done before testing the gallery.

- [ ] **Step 1: Enable public access on the R2 bucket**

In the Cloudflare dashboard:
1. Left sidebar → **R2 Object Storage** → click `boda-fotos`
2. Click the **Settings** tab
3. Find the **Public access** section
4. Click **Allow Access** (or toggle to enable)
5. Cloudflare will assign a URL like `https://pub-<hash>.r2.dev`
6. **Copy and save this URL** — you'll need it in Task 3

- [ ] **Step 2: Test that a file is publicly accessible**

Pick any key you uploaded earlier (visible in the R2 Objects tab, e.g. `1776425056864-4f74ea4e-....jpg`). Open in your browser:

```
https://pub-<hash>.r2.dev/<key>
```

Expected: the image loads directly.

- [ ] **Step 3: Update Worker code for GET /list**

Replace the full content of `cloudflare-worker/index.js` with:

```javascript
const CORS_HEADERS = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Methods': 'POST, GET, OPTIONS',
  'Access-Control-Allow-Headers': 'Content-Type',
};

export default {
  async fetch(request, env) {
    if (request.method === 'OPTIONS') {
      return new Response(null, { headers: CORS_HEADERS });
    }

    const url = new URL(request.url);

    // GET /list — return bucket contents sorted newest-first
    if (request.method === 'GET' && url.pathname === '/list') {
      try {
        const listed = await env.BODA_BUCKET.list();
        const files = listed.objects
          .map(obj => ({
            key: obj.key,
            size: obj.size,
            uploaded: obj.uploaded.toISOString(),
          }))
          .sort((a, b) => new Date(b.uploaded) - new Date(a.uploaded));

        return new Response(JSON.stringify({ files }), {
          headers: { ...CORS_HEADERS, 'Content-Type': 'application/json' },
        });
      } catch (err) {
        return new Response(JSON.stringify({ error: String(err) }), {
          status: 500,
          headers: { ...CORS_HEADERS, 'Content-Type': 'application/json' },
        });
      }
    }

    // POST / — upload file to R2
    if (request.method === 'POST') {
      const filename = url.searchParams.get('filename') || 'upload.bin';
      const contentType = request.headers.get('Content-Type') || 'application/octet-stream';
      const ext = filename.split('.').pop() || 'bin';
      const key = `${Date.now()}-${crypto.randomUUID()}.${ext}`;

      try {
        await env.BODA_BUCKET.put(key, request.body, {
          httpMetadata: { contentType },
        });
        return new Response(JSON.stringify({ key }), {
          headers: { ...CORS_HEADERS, 'Content-Type': 'application/json' },
        });
      } catch (err) {
        return new Response(JSON.stringify({ error: String(err) }), {
          status: 500,
          headers: { ...CORS_HEADERS, 'Content-Type': 'application/json' },
        });
      }
    }

    return new Response('Method not allowed', { status: 405, headers: CORS_HEADERS });
  },
};
```

- [ ] **Step 4: Deploy updated Worker**

In Cloudflare dashboard:
1. **Workers & Pages** → `boda-fotos-presign` → **Edit code**
2. Replace all code with the content above
3. Click **Deploy**

- [ ] **Step 5: Test the /list endpoint**

```bash
curl https://boda-fotos-presign.jaimelega4.workers.dev/list
```

Expected response (files you've already uploaded):
```json
{"files":[{"key":"1776425056864-4f74ea4e-....jpg","size":245760,"uploaded":"2026-04-17T11:24:16.000Z"}]}
```

- [ ] **Step 6: Commit updated Worker file**

```bash
cd "/Users/jaime.jimenez/Library/CloudStorage/OneDrive-Allianz/Documents/personal documents/wedding-inv"
git add cloudflare-worker/index.js
git commit -m "feat: add GET /list endpoint to Worker for gallery"
```

---

## Task 2: Gallery Service

**Files:**
- Create: `src/app/services/gallery.service.ts`

- [ ] **Step 1: Create the service**

Create `src/app/services/gallery.service.ts`:

```typescript
import { Injectable } from '@angular/core';

export interface GalleryFile {
  key: string;
  size: number;
  uploaded: string;
  isImage: boolean;
  isVideo: boolean;
  filename: string;
}

const IMAGE_EXTS = new Set(['jpg', 'jpeg', 'png', 'gif', 'webp', 'heic', 'heif']);
const VIDEO_EXTS = new Set(['mp4', 'mov', 'avi', 'mkv', 'webm']);

function detectType(key: string): { isImage: boolean; isVideo: boolean } {
  const ext = key.split('.').pop()?.toLowerCase() ?? '';
  return { isImage: IMAGE_EXTS.has(ext), isVideo: VIDEO_EXTS.has(ext) };
}

function extractFilename(key: string): string {
  // Keys are formatted as "timestamp-uuid.ext" — return just the extension part as a human label
  const parts = key.split('.');
  const ext = parts.pop() ?? '';
  return ext ? `archivo.${ext}` : key;
}

@Injectable({ providedIn: 'root' })
export class GalleryService {
  private workerUrl = 'https://boda-fotos-presign.jaimelega4.workers.dev';

  async listFiles(): Promise<GalleryFile[]> {
    const response = await fetch(`${this.workerUrl}/list`);
    if (!response.ok) {
      throw new Error(`Worker error: ${response.status}`);
    }
    const data: { files: { key: string; size: number; uploaded: string }[] } = await response.json();
    return data.files.map(f => ({
      ...f,
      ...detectType(f.key),
      filename: extractFilename(f.key),
    }));
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/services/gallery.service.ts
git commit -m "feat: add GalleryService to list R2 bucket files"
```

---

## Task 3: Gallery Component — Logic

**Files:**
- Create: `src/app/components/gallery/gallery.component.ts`

- [ ] **Step 1: Create the component**

Create `src/app/components/gallery/gallery.component.ts`:

```typescript
import { Component, OnInit } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { GalleryService, GalleryFile } from '../../services/gallery.service';

@Component({
  selector: 'app-gallery',
  standalone: true,
  imports: [CommonModule, RouterModule],
  templateUrl: './gallery.component.html',
  styleUrls: ['./gallery.component.css'],
})
export class GalleryComponent implements OnInit {
  // Replace with actual public URL after enabling R2 public access in dashboard
  readonly r2PublicUrl = 'https://pub-REPLACE_WITH_YOUR_HASH.r2.dev';

  files: GalleryFile[] = [];
  loading = true;
  error: string | null = null;

  constructor(private galleryService: GalleryService) {}

  ngOnInit(): void {
    this.loadFiles();
  }

  async loadFiles(): Promise<void> {
    this.loading = true;
    this.error = null;
    try {
      this.files = await this.galleryService.listFiles();
    } catch {
      this.error = 'No se pudo cargar la galería, inténtalo de nuevo';
    } finally {
      this.loading = false;
    }
  }

  downloadUrl(key: string): string {
    return `${this.r2PublicUrl}/${key}`;
  }

  get imageCount(): number {
    return this.files.filter(f => f.isImage).length;
  }

  get videoCount(): number {
    return this.files.filter(f => f.isVideo).length;
  }
}
```

> **Important:** After completing Task 1 Step 1, replace `pub-REPLACE_WITH_YOUR_HASH` in `r2PublicUrl` with the actual URL from the Cloudflare dashboard.

- [ ] **Step 2: Commit**

```bash
git add src/app/components/gallery/gallery.component.ts
git commit -m "feat: add GalleryComponent logic"
```

---

## Task 4: Gallery Component — Template

**Files:**
- Create: `src/app/components/gallery/gallery.component.html`

- [ ] **Step 1: Create the template**

Create `src/app/components/gallery/gallery.component.html`:

```html
<main class="bg-background min-h-screen">
  <section class="section-padding">
    <div class="max-w-5xl mx-auto px-4 w-full">

      <!-- Header -->
      <div class="text-center mb-8 w-full">
        <h1 class="font-script text-5xl text-ink mb-2">Galería</h1>
        <p class="text-ink/70 font-body text-sm tracking-wide mb-1">
          Todos los recuerdos del día
        </p>
        <p *ngIf="!loading && !error && files.length" class="text-ink/50 font-body text-xs">
          {{ imageCount }} fotos · {{ videoCount }} vídeos
        </p>
      </div>

      <!-- Loading -->
      <div *ngIf="loading" class="flex flex-col items-center justify-center py-24 gap-4">
        <div class="gallery-spinner"></div>
        <p class="text-ink/50 font-body text-sm">Cargando recuerdos...</p>
      </div>

      <!-- Error -->
      <div *ngIf="!loading && error" class="flex flex-col items-center justify-center py-24 gap-4">
        <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24"
          fill="none" stroke="currentColor" stroke-width="1.5" class="text-ink/30">
          <circle cx="12" cy="12" r="10"/>
          <line x1="12" y1="8" x2="12" y2="12"/>
          <line x1="12" y1="16" x2="12.01" y2="16"/>
        </svg>
        <p class="text-ink/70 font-body text-sm text-center">{{ error }}</p>
        <button (click)="loadFiles()"
          class="px-4 py-2 text-sm border-b-2 border-sage-dark text-sage-dark font-body
                 hover:text-sage-dark/70 hover:border-sage-dark/70 transition-colors bg-transparent">
          Reintentar
        </button>
      </div>

      <!-- Empty -->
      <div *ngIf="!loading && !error && !files.length"
        class="flex flex-col items-center justify-center py-24 gap-4">
        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24"
          fill="none" stroke="currentColor" stroke-width="1.5" class="text-ink/20">
          <rect width="18" height="18" x="3" y="3" rx="2" ry="2"/>
          <circle cx="9" cy="9" r="2"/>
          <path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/>
        </svg>
        <p class="text-ink/50 font-body text-sm text-center">
          Aún no hay fotos. ¡Sé el primero en subir!
        </p>
        <a routerLink="/fotos"
          class="px-4 py-2 text-sm border-b-2 border-sage-dark text-sage-dark font-body
                 hover:text-sage-dark/70 hover:border-sage-dark/70 transition-colors">
          Subir fotos
        </a>
      </div>

      <!-- Gallery grid -->
      <div *ngIf="!loading && !error && files.length" class="gallery-grid">
        <div *ngFor="let file of files; let i = index"
          class="gallery-item"
          [style.animation-delay]="(i * 50) + 'ms'">

          <!-- Image -->
          <img *ngIf="file.isImage"
            [src]="downloadUrl(file.key)"
            [alt]="file.filename"
            class="gallery-thumb"
            loading="lazy" />

          <!-- Video placeholder -->
          <div *ngIf="file.isVideo" class="gallery-thumb gallery-video-placeholder">
            <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24"
              fill="none" stroke="currentColor" stroke-width="1.5">
              <polygon points="5 3 19 12 5 21 5 3"/>
            </svg>
          </div>

          <!-- Unknown type placeholder -->
          <div *ngIf="!file.isImage && !file.isVideo" class="gallery-thumb gallery-unknown-placeholder">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"
              fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M13 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V9z"/>
              <polyline points="13 2 13 9 20 9"/>
            </svg>
          </div>

          <!-- Hover overlay -->
          <div class="gallery-overlay">
            <p class="gallery-overlay-name">{{ file.filename }}</p>
            <a [href]="downloadUrl(file.key)"
              download
              target="_blank"
              class="gallery-download-btn">
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24"
                fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                <polyline points="7 10 12 15 17 10"/>
                <line x1="12" x2="12" y1="15" y2="3"/>
              </svg>
              Descargar
            </a>
          </div>

        </div>
      </div>

    </div>
  </section>
</main>
```

- [ ] **Step 2: Commit**

```bash
git add src/app/components/gallery/gallery.component.html
git commit -m "feat: add GalleryComponent template"
```

---

## Task 5: Gallery Component — Styles

**Files:**
- Create: `src/app/components/gallery/gallery.component.css`

- [ ] **Step 1: Create the CSS file**

Create `src/app/components/gallery/gallery.component.css`:

```css
/* Grid layout */
.gallery-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 0.5rem;
}

@media (min-width: 640px) {
  .gallery-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (min-width: 1024px) {
  .gallery-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}

/* Grid item */
.gallery-item {
  position: relative;
  aspect-ratio: 1;
  overflow: hidden;
  border-radius: 0.5rem;
  background-color: var(--color-ivory);
  cursor: pointer;
  animation: fadeIn 0.4s ease both;
}

/* Thumbnail */
.gallery-thumb {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform 0.4s ease;
}

.gallery-item:hover .gallery-thumb {
  transform: scale(1.05);
}

/* Video placeholder */
.gallery-video-placeholder {
  background-color: var(--color-ink);
  display: flex;
  align-items: center;
  justify-content: center;
  color: rgba(255, 255, 255, 0.5);
}

/* Unknown type placeholder */
.gallery-unknown-placeholder {
  background-color: rgba(44, 36, 22, 0.08);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-ink-light);
}

/* Hover overlay */
.gallery-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to top, rgba(44, 36, 22, 0.85) 0%, rgba(44, 36, 22, 0.2) 50%, transparent 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-end;
  padding: 0.75rem;
  gap: 0.5rem;
  opacity: 0;
  transition: opacity 0.25s ease;
}

.gallery-item:hover .gallery-overlay {
  opacity: 1;
}

/* File name in overlay */
.gallery-overlay-name {
  color: rgba(255, 255, 255, 0.8);
  font-family: 'Lora', serif;
  font-size: 0.7rem;
  text-align: center;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Download button */
.gallery-download-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  padding: 0.35rem 0.75rem;
  background: rgba(255, 255, 255, 0.15);
  border: 1px solid rgba(255, 255, 255, 0.4);
  border-radius: 2rem;
  color: white;
  font-family: 'Lora', serif;
  font-size: 0.7rem;
  text-decoration: none;
  backdrop-filter: blur(4px);
  transition: background 0.2s ease, border-color 0.2s ease;
}

.gallery-download-btn:hover {
  background: rgba(255, 255, 255, 0.28);
  border-color: rgba(255, 255, 255, 0.7);
}

/* Loading spinner */
.gallery-spinner {
  width: 32px;
  height: 32px;
  border: 2px solid rgba(76, 105, 81, 0.2);
  border-top-color: var(--color-sage-dark);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

/* Animations */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes spin {
  to { transform: rotate(360deg); }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/components/gallery/gallery.component.css
git commit -m "feat: add GalleryComponent styles with hover overlay and animations"
```

---

## Task 6: Register Route

**Files:**
- Modify: `src/app/app.routes.ts`

- [ ] **Step 1: Add the `/galeria` route**

Replace the full content of `src/app/app.routes.ts` with:

```typescript
import { Routes } from '@angular/router';
import { VideoEnvelopeComponent } from './components/video-envelope/video-envelope.component';
import { HomeComponent } from './components/home/home.component';
import { PhotoUploadComponent } from './components/photo-upload/photo-upload.component';
import { GalleryComponent } from './components/gallery/gallery.component';

export const routes: Routes = [
  { path: '', component: VideoEnvelopeComponent },
  { path: 'home', component: HomeComponent },
  { path: 'fotos', component: PhotoUploadComponent },
  { path: 'galeria', component: GalleryComponent },
  { path: '**', redirectTo: '' },
];
```

- [ ] **Step 2: Verify the app compiles**

```bash
cd "/Users/jaime.jimenez/Library/CloudStorage/OneDrive-Allianz/Documents/personal documents/wedding-inv"
npm run build 2>&1 | tail -20
```

Expected: build succeeds with no errors.

- [ ] **Step 3: Commit**

```bash
git add src/app/app.routes.ts
git commit -m "feat: register /galeria route for photo gallery"
```

---

## Task 7: Set R2 Public URL + Manual End-to-End Test

- [ ] **Step 1: Set the real R2 public URL in the component**

Open `src/app/components/gallery/gallery.component.ts`.

Find:
```typescript
readonly r2PublicUrl = 'https://pub-REPLACE_WITH_YOUR_HASH.r2.dev';
```

Replace `pub-REPLACE_WITH_YOUR_HASH` with the actual URL from Task 1 Step 1.

Example:
```typescript
readonly r2PublicUrl = 'https://pub-abc123def456.r2.dev';
```

- [ ] **Step 2: Start the dev server**

```bash
npm start
```

- [ ] **Step 3: Open the gallery on desktop**

Open `http://localhost:4200/galeria` in your browser.

Expected:
- Grid of photos loads with fade-in animation
- Hovering a photo shows dark overlay with filename and download button
- Video items show dark placeholder with play icon
- File count shows correctly in header

- [ ] **Step 4: Test download**

Hover a photo → click "Descargar". Expected: file downloads to your computer.

- [ ] **Step 5: Test on mobile**

Find your laptop's IP: `ipconfig getifaddr en0`

Open `http://<your-ip>:4200/galeria` on your phone.

Expected: grid renders correctly, download works on mobile.

- [ ] **Step 6: Final commit**

```bash
git add src/app/components/gallery/gallery.component.ts
git commit -m "feat: gallery feature complete with R2 public URL configured"
```
